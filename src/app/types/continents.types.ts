export interface ContinentCardType {
  name: string;
  code: string;
}

export interface CountriesCardType {
  name: string;
  code: string;
}

export interface CountryCardType {
  name: string;
  capital: string;
  emoji: string;
  currency: string;
}